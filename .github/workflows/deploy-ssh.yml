name: Build and deploy
on:
  push:
    branches:
      - main
  workflow_dispatch:
permissions:
  contents: read
concurrency:
  group: deploy
  cancel-in-progress: true
defaults:
  run:
    shell: bash
jobs:
  build:
    runs-on: ubuntu-latest
    env:
      DART_SASS_VERSION: 1.90.0
      HUGO_VERSION: 0.160.1
      TZ: Europe/Moscow
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
    steps:
      - name: Checkout
        uses: actions/checkout@v5
        with:
          submodules: recursive
          fetch-depth: 0
      - name: Create directory for user-specific executable files
        run: |
          mkdir -p "${HOME}/.local"
      - name: Install Dart Sass
        run: |
          curl -sLJO "https://github.com/sass/dart-sass/releases/download/${DART_SASS_VERSION}/dart-sass-${DART_SASS_VERSION}-linux-x64.tar.gz"
          tar -C "${HOME}/.local" -xf "dart-sass-${DART_SASS_VERSION}-linux-x64.tar.gz"
          rm "dart-sass-${DART_SASS_VERSION}-linux-x64.tar.gz"
          echo "${HOME}/.local/dart-sass" >> "${GITHUB_PATH}"
      - name: Install Hugo
        uses: peaceiris/actions-hugo@v3
        with:
          hugo-version: ${{ env.HUGO_VERSION }}
          extended: true
      - name: Install rclone
        run: |
          sudo apt-get update -y
          sudo apt-get install -y rclone
      - name: Verify installations
        run: |
          echo "Dart Sass: $(sass --version)"
          echo "Hugo: $(hugo version)"
          echo "rclone: $(rclone --version)"
      - name: Configure Git
        run: |
          git config core.quotepath false
      - name: Cache restore
        id: cache-restore
        uses: actions/cache/restore@v4
        with:
          path: ${{ runner.temp }}/hugo_cache
          key: hugo-${{ env.HUGO_VERSION }}-${{ github.sha }}
          restore-keys: |
            hugo-${{ env.HUGO_VERSION }}-
      - name: Build the site
        run: |
          hugo \
            --gc \
            --minify \
            --baseURL https://gurfoto.ru/ \
            --cacheDir "${{ runner.temp }}/hugo_cache"
      - name: Cache save
        id: cache-save
        uses: actions/cache/save@v4
        with:
          path: ${{ runner.temp }}/hugo_cache
          key: ${{ steps.cache-restore.outputs.cache-primary-key }}
      - name: Deploy with rclone
        run: |
          RCLONE_SFTP_PASS="$(rclone obscure "${{ secrets.SFTP_PASSWORD }}")"
          rclone sync ./public :sftp:${{ secrets.SFTP_TARGET_DIR }} \
            --sftp-host=${{ secrets.SFTP_HOST }} \
            --sftp-user=${{ secrets.SFTP_USERNAME }} \
            --sftp-pass="$RCLONE_SFTP_PASS" \
            --exclude "/vtour/**" \
            --stats 1s
